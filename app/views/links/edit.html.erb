<% form_for @link, :url => [@park, @link] do |f| %>
  <p class="name">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>

  <p class="href">
    <%= f.label :url, "URL" %>
    <%= f.text_field :url %>
  </p>

  <p>
    <%= f.submit 'Update' %>
  </p>

<% end %>
